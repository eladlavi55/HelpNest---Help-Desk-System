# Server
PORT=4001

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/support

# Auth (required)
JWT_ACCESS_SECRET=replace_me
JWT_REFRESH_SECRET=replace_me

# CORS (default: http://localhost:3000)
WEB_ORIGIN=http://localhost:3000

# Support agents: users whose email domain is in this list get isSupportAgent=true
# and are added to Support Ops workspace as ADMIN. Comma-separated (e.g. demo.local).
# Seed creates admin@demo.local with isSupportAgent=true regardless.
SUPPORT_AGENT_EMAIL_DOMAINS=demo.local